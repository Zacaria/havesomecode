{
  "name": "i18node",
  "version": "1.0.0",
  "description": "i18n try with nodejs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "gulp",
    "deploy": "npm run clean && cd docs && npm run build && ls && npm run copyAssets && npm run surge",
    "surge": "surge -d $npm_package_config_deploy_domain -p docs/_static",
    "clean": "rimraf build",
    "printpdf": "mkdir -p build && node printPdf.js",
    "build": "npm-run-all --parallel --race start printpdf"
  },
  "repository": {
    "type": "git",
    "url": "Zacaria"
  },
  "keywords": [
    "i18n",
    "nodejs",
    "express",
    "pug"
  ],
  "author": "Zacaria",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3",
    "i18next": "^1.10.4",
    "pug": "^2.0.3",
    "puppeteer": "^1.6.1"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "gulp": "^3.9.1",
    "gulp-nodemon": "^2.2.1",
    "gulp-sass": "^4.0.1",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2"
  },
  "config": {
    "deploy_domain": "havesome-resume.surge.sh"
  }
}
